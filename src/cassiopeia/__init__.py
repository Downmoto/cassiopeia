"""Core cassiopeia package.

The package contains application primitives used by every gateway and admin
surface. Gateway-specific entry points, including the CLI, should call into this
package instead of owning core behaviour directly.
"""
